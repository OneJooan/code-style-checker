<?php
// example.php - contains all style issues

// [TAB] tab indentation
function greet($name) {
	return "Hello, " . $name;
}

// [TRAILING SPACE] trailing spaces after code
$x = 42;   

// [MIXED INDENT] spaces then tab
function add($a, $b) {
     	return $a + $b;
}

// [MIXED INDENT] tab then space
function sub($a, $b) {
	 return $a - $b;
}
